import random

# Variable for ending the game 
endgame = False  # Change to a boolean for proper condition checking

# Ask user for name
def play_game(player_name):
    print(f"\n{player_name} is now playing!")
    # Game logic goes here
    
    global endgame  
    if endgame:
        print("Game over for {player_name}!")

def ran_number ():
    return random.randint (1,100)
    
def guess_checker ():
       player_guess = input("Guess a number between 1-100:")
       if 

def main():
    while True:
        player_name = input("Enter player's name: ")
        play_game(player_name)
        guess_checker()
        
        cont = input("Do you want another player to play? (y/n): ")
        if cont != 'y':
            print("Thanks for playing!")
            break
        
if __name__ == "__main__":
    main()
